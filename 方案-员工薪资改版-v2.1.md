# 方案：员工薪资改版（v2.1 方案稿）

> **状态**：已确认，原型按本方案执行  
> **拍板来源**：2026-08-31 问卷 + 最终确认（服务=项目+快消；售卡=办卡+充卡）  
> **压缩串**：A1a A2a A3c A4a A5a A6a A7a · B1a B2b B3a B4c B5a B6c B7a B8a B9a · C1a C2a C3a C4a C5a · D1a D2a D3a D4a · E1a E2a

---

## 0. 目标一览

| # | 改动 | 摘要 |
|---|------|------|
| 1 | 员工薪资列表 | 删「生效中」整块；结算周期页增加发薪日；卡面「合计」左置；下半 2×2 提成四格（无进度条） |
| 2 | **新页**员工薪资明细 | 头像+姓名+月份 → 薪资卡 → 业绩统计 → 提成统计 → 服务/售卡 → 奖惩摘要 |
| 3 | 提成修改 | 取消待确认全流程；店主改提成直接生效；业绩只读；字段旁留痕 |
| 4 | 员工业绩提成明细 | 月合计字段与薪资明细一致；日列表精简；删待确认筛 |

---

## 1. 信息架构与导航

```
员工薪资（列表）
 ├─ 顶：全店合计 / 月份 / 结算周期入口（含发薪日只读摘要）
 ├─ 卡 →【员工薪资明细】（新，B8-a）
 │     ├─ 奖惩「查看全部」→ 现有奖惩明细页
 │     └─（可选入口）→ 员工业绩提成明细
 └─ 标题「设奖惩」→ 现有设奖惩

员工业绩提成明细（改版）
 ├─ 店主/员工视角切换保留（员工无审批）
 └─ 行内提成可改（店主）→ 直接生效 + 留痕

删除导航演示入口：
 ✕ 明细·待确认
 ✕ 明细·员工确认
```

**列表卡点击**：整卡进入「员工薪资明细」（不再直达业绩提成明细）。  
**业绩提成明细入口**（建议，确认时可改）：薪资明细页标题栏次级文字按钮「业绩提成明细」，或卡片内「查看逐笔」。

---

## 2. 员工薪资列表

### 2.1 顶部

**删除**（A1-a）：`生效中` 标题 + tone chips + 展开按钮整块。

**保留**：

```
┌─────────────────────────────────────┐
│ 本期全店合计薪酬                      │
│ ¥128,560.00              [2026年08月▾]│
│ 自然月 · 08.01–08.31 · 发薪日 12日 ⚙  │  ← 周期入口，只读展示发薪日
└─────────────────────────────────────┘
```

### 2.2 发薪日（A2-a / A3-c / A4-a）

**设置位置**：现有「结算周期」页，与结算日**同级**（上下相邻两行或左右并排）。

**权限**：仅店主（合伙人若现网等同店主）；店长/员工只读。

**默认推算（A3-c，按「每月 30 日」抽象月）**

1. 设结算日为每月第 `S` 日（1–30，与现网结算日配置对齐；若现网允许 31，先映射到 30 日制再算）。  
2. `raw = S + 12`。  
3. 若 `raw ≤ 30` → `payDayOfMonth = raw`（相对结算日后的「同周期偏置」仅用于推导默认日号）。  
4. 若 `raw > 30` → `payDayOfMonth = raw - 30`。  
5. **落库只存「每月第 N 日」** `payDayOfMonth ∈ [1,30]`。  
6. **实际日历**：每个自然月发薪落在「该月日期 = N」；若该月天数 `< N`（如 N=30 的 2 月），**夹到该月最后一天**，但仍视为规则上的「第 N 日」配置不变——保证配置层面每月同一「日」号。

例：`S=20` → `payDay=32→2` → 每月 **2 日**发薪。  
例：`S=5` → `payDay=17` → 每月 **17 日**发薪。

**交互**：改结算日时，若发薪日仍为「系统默认未手动改过」，则按公式重算；若店主曾手动改过发薪日，则保留手改值（可提供「恢复默认」）。

### 2.3 员工卡

**上半（A5-a）**

```
[头像] 张三          合计  ¥12,580.00  ›
```

- 文案「薪资合计」→「合计」，与金额同一行、金额左侧。  
- 右 chevron 表示可进明细。

**下半（A6-a / A7-a）**：去掉基本工资/提成合计/奖惩/劳动业绩等旧行与**数值条**；改为 **2×2**，值为该员工**本期提成**分列：

| 项目 | 产品 |
| 办卡/充卡 | 快速消费 |

- **办卡/充卡**：办卡提成 + 充卡提成之和（展示一格）。  
- **快速消费**：单独一格。  
- **项目**：项目类提成 − 快速消费提成（与 PRD 提成「项目」净值一致），避免与快消双计。  
- 图标：复用提成设置规则卡 SVG（办卡/充卡格可用办卡图标或双色小标）。  
- 无 meter / 无进度条；四格等宽、对齐、左图标+标题+右金额。

线框：

```
┌──────────────────────────────────┐
│ [头] 李娜          合计 ¥8,320.00 ›│
├────────────────┬─────────────────┤
│ 🛠 项目  ¥1,200 │ 📦 产品  ¥860   │
│ 💳 办卡/充卡 ¥2,1│ ⚡ 快速消费 ¥340│
└────────────────┴─────────────────┘
```

---

## 3. 新页：员工薪资明细

### 3.1 第一部分 · 身份与月份（B9-a）

```
‹ 员工薪资明细
┌────────────────────────────────┐
│ [大头像]  李娜                   │
│           高级美容师             │
│                    [2026年08月▾] │  ← 复用 empMonthMask
└────────────────────────────────┘
```

### 3.2 第二部分 · 薪资卡（B5-a）

```
┌────────────────────────────────┐
│ 当月合计薪资                      │
│ ¥8,320.00                        │
│────────────────────────────────│
│ 基本薪资   提成总计   奖惩总计     │
│ ¥4,000    ¥4,520    ¥−200       │
└────────────────────────────────┘
```

- 奖惩总计 = 奖 − 惩（净额）；负值用警示色。  
- 合计 = 基本 + 提成 + 奖惩净额（与列表一致）。

### 3.3 第三部分 · 业绩统计（B1-a / B2-b / B4-c）

**分列（支付方式，四格）**：

| 字段 | 口径 |
|------|------|
| 现金 | 该支付方式下相关开单的**原价加总**（业绩统计仍用原价，与提示词第 2 条「业绩」一致） |
| 卡付 | 同上 |
| 团购 | 同上 |
| **签单** | **经理签单 + 记为欠款** 合并一格，原价加总 |

**UI（B4-c）**：顶部「总计」大数字；下方四行（或两列）**横向比例条**（非圆环），条长 = 该项 / 总计。

```
业绩统计
总计  ¥52,000.00
现金   ████████░░  ¥22,000
卡付   ██████░░░░  ¥15,000
团购   ████░░░░░░  ¥10,000
签单   ██░░░░░░░░   ¥5,000
```

### 3.4 第四部分 · 提成统计（B3-a / B4-c）

**五类分开**（不与列表 2×2 合并逻辑混用）：

- 项目 / 产品 / 办卡 / 充卡 / 快速消费  
- 项目列展示值 = 项目类提成 − 快速消费提成  
- 五列之和 = 提成总计（= 上方「提成总计」）

UI 同业绩：总计 + 横向比例条。

### 3.5 第五部分 · 服务 / 售卡（B6-c）

```
┌──────────────┬──────────────┐
│ 服务          │ 售卡          │
│ 人次 128      │ 数量 36       │
│ 金额 ¥xx（实收）│ 金额 ¥yy（实收）│
└──────────────┴──────────────┘
```

- **映射（已确认）**：  
  - **服务** = 项目 + 快速消费（人次与**实收**金额）。  
  - **售卡** = 办卡 + 充卡（数量与**实收**金额；**不含产品**）。  
- 原型造数按上表拆；PRD 写明「金额口径为实收」。

### 3.6 第六部分 · 奖惩明细（B7-a）

- 页内最近 **5** 条：日期、类型（奖/惩）、金额、备注摘要。  
- 右上「查看全部」→ 现有 `screen-emp-reward-detail`（带当前员工/月份上下文）。

---

## 4. 提成修改：直接生效 + 留痕（C）

### 4.1 取消（C1-a）

删除/下线：

- 待确认草稿态、同意/驳回/撤回  
- 相关 Dialog / hint「提交待确认」  
- 导航「明细·待确认」「明细·员工确认」  
- 列表「待确认」筛选与角标  

### 4.2 可编辑性（C2-a）

| 字段 | 店主/合伙人 | 员工视角 |
|------|-------------|----------|
| 业绩 | **只读** | 只读 |
| 提成 | **可改，保存即生效** | 只读 |

编辑入口：员工业绩提成明细逐笔行、以及现有流水侧提成编辑（若本包有）统一为直接生效。

### 4.3 留痕（C3-a / C4-a / C5-a）

- 被改过的**提成**旁显示小标记（如「改」点或铅笔点）。  
- 点击打开**修改记录半屏 Sheet**：  
  - 时间线：原值 → 新值、操作人、时间、原因（选填，可空）  
  - 底部提示文案：「该调整已生效。如有疑问请线下与店主核实。」  
- 员工视角：可打开记录，**无**同意/驳回按钮。

数据：每条订单行/明细行挂 `commEditLogs[]`：`{ from, to, by, at, reason? }`。

---

## 5. 员工业绩提成明细改版（D）

### 5.1 月合计（D1-a）

与 §3.3 / §3.4 **完全一致**：

- 业绩：现金 / 卡付 / 团购 / 签单（签单=经理签单+欠款），原价  
- 提成：项目 / 产品 / 办卡 / 充卡 / 快速消费，项目扣快消  

可用更紧凑的「月合计」双区块（业绩一块、提成一块），比例条或等宽四/五列数字，避免旧版杂乱色条。

### 5.2 日列表（D2-a）

```
08月28日                    业绩 ¥x  提成 ¥y
  14:20  深层补水护理    业绩 268  提成 48.24 [改]
  16:01  产品·精华液     业绩 198  提成 19.80
```

- 行：时间 · 名称 · 业绩 · 提成；有留痕则角标。  
- 点击提成（店主）→ 编辑 Sheet → 保存生效。

### 5.3 筛选与视角（D3-a / D4-a）

- 删「待确认」筛；保留日期范围、类型（项目/产品等）若已有。  
- 保留「员工视角」切换；员工无审批，仅看留痕。

---

## 6. PRD v2.1 同步要点（E1-a）

确认原型后写入 `PRD-提成设置.md`：

1. **§6.10.1** 员工薪资明细：新建页规格；业绩统计改为支付四格（签单=签单+欠款）；提成五类；服务/售卡金额=实收；发薪日。  
2. **§6.10.2** 业绩提成明细：与上对齐；取消确认流；留痕。  
3. **业务规则**：提成可改直接生效；业绩不可编；发薪日 30 日制默认公式。  
4. 验收用例增补列表卡 2×2、发薪日权限、留痕、无待确认等。

---

## 7. 原型改造清单（确认后执行）

| 文件 | 改动 |
|------|------|
| `index.html` / `salary-fragment.html` | 删生效中 DOM；新 screen 员工薪资明细；结算周期增发薪日；去确认相关 picker |
| `salary.css` | 列表卡 2×2、合计左置、明细页各区块、比例条、留痕标 |
| `employee.js` | 列表渲染、发薪日状态、明细页 render、合并签单口径、取消 pending 流、commEditLogs |
| `app.js` / 导航 | 新 flow、删待确认入口；卡点击路由 |
| `PRD-提成设置.md` + `.html` | v2.1 |

造数：为演示员工生成四类业绩、五类提成、服务/售卡实收、若干奖惩与 1～2 条提成修改留痕。

---

## 8. 已确认默认值

1. **业绩提成明细入口**：薪资明细顶栏次级按钮「业绩提成明细」—— OK  
2. **服务/售卡映射**：服务 = 项目 + 快消（人次/实收）；售卡 = 办卡 + 充卡（数量/实收，**不含产品**）—— OK  
3. **改结算日后发薪日**：未手改则跟公式重算；已手改则保留—— OK
