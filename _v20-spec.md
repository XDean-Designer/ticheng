# 提成设置 v2.0 · 拍板规格（2026-08-31）

> 来源：`_v20-questionnaire.html` 问卷 + 用户补充说明。

---

## 一、拍板汇总

| 题号 | 选项 | 结论 |
|------|------|------|
| A1 | a | 删列表 signComm UI；删 `signComm` 字段；删试算专用逻辑中的方案级开关 |
| A2 | a | PRD/验收删除 v1.8 signComm 相关（规则 29、用例 51–54 等），重写口径 |
| B1–B4 | 全 a | 右列标签「基数」「范围」；范围用独立 chip 多选；保留卡面直改；固定金额显示「固定」 |
| C1–C4 | 全 a | 标题→全量 Sheet；参数→无范围/基数；添加规则项→全量；参数 Sheet 顶即参数区 |
| D1 | a | 迁移：结构必迁，**不要求金额一致**，差异报告，**不阻断上线** |
| D2 | a | 丢弃 cost/achPct；能直写则写；其余 override 取旧 comm **原值**（不折叠） |
| D3 | a | 「不计算」→ rate/amount 全 0，payScope 仍三类全开 |
| D4 | a | 充/续/续费不一致→能识别则多条覆盖，否则充卡默认+报告 |
| D5 | a | 特殊项最近似分类+报告；无法识别跳过 |
| **D6** | **自定义** | 经理签单 = **实收为 0**；是否计提由命中规则块 **`baseMode` 决定**：`list`（原价）→ 按原价计；`paid`（实收）→ 基数 0 不计。不走 payScope 三类过滤 |
| **D7** | **自定义** | **阶梯暂不迁入**；后续在原型中制作阶梯提成功能，实现全面迭代；现网阶梯仍走旧引擎 |
| D8 | a | 一套旧单项方案 → 一张新 Scheme |
| **D9** | **c** | **无回滚开关**；迁移后统一读新引擎 |
| D10 | a | 删除层 A–E 金额门禁；改为**结构校验 + 迁移报告** |
| E1 | a | PRD **v2.0** |
| E2 | a | §6.2 规则卡整节重写 |

### 用户补充 · 迁移结果告知（产品行为）

1. **有差异**：迁移完成后弹窗提醒「计算结果与旧方案存在差异」+ 提供**重新创建方案**入口（引导用户手动校正）。
2. **无差异（完美迁移）**：同样弹窗一次，告知「已完美迁移，计算结果一致」。

> 原型：可在方案列表首次进入或工具栏演示「模拟迁移结果」两种弹窗；正式产品由迁移 Job 触发。

---

## 二、原型改动清单

### 2.1 方案列表
- [ ] 删除 `comm2-signcomm` 行及相关 CSS/事件/toast
- [ ] 删除 `Scheme.signComm` 字段及 normalize 兜底

### 2.2 计提引擎（经理签单 · D6）
- [ ] 删除 `signComm` 判断
- [ ] `line.sign === true`：跳过 payScope；`baseMode=list` → `base=line.list` 正常计；`baseMode=paid` → `base=0`（实收为 0）→ 比例提成为 0；固定金额仍取固定额
- [ ] 试算行 `tl9` 保留；note 文案改为「实收模式下经理签单不计提成」等

### 2.3 规则卡 UI（§6.2）· 二次确认 L1+T1+V1
- [x] 上控下参（方案 A）：顶行标题+基数/范围；底行参数横跨整卡
- [x] 「基数」「范围」与控件同行；基数整段切换；参数与标题文字左缘对齐
- [x] 删除发丝线

### 2.4 Sheet 拆分
- [ ] `openCardSheet(target, { variant: 'full'|'params' })`
- [ ] full：范围+基数+分配+提成参数（现逻辑）
- [ ] params：仅分配+提成参数+会员卡（覆盖时）
- [ ] 添加规则项 Sheet：full

### 2.5 迁移弹窗（演示）
- [ ] Dialog：迁移差异 / 完美迁移 两种文案 + 「重新创建方案」按钮（差异时）

---

## 三、PRD v2.0 变更目录

1. 版本 v2.0 blurb
2. 删除 signComm 全篇；§8 新增经理签单按 baseMode 规则
3. §6.1 方案卡结构（无 signComm 行）
4. §6.2 规则卡 UI 重写（标签/chip/无发丝线/Sheet 拆分）
5. §7.1 删 signComm 字段
6. §10 验收用例更新
7. **模块 13 重写**：目标改为结构迁移；删等价门禁；D6/D7/D9；迁移弹窗 UX

---

## 四、实施顺序

1. comm2.js + comm2.css（引擎 + UI + Sheet）
2. index.html 迁移演示 Dialog（可选）
3. PRD-提成设置.md → HTML 再生
4. 删除临时问卷页（可选，用户未要求）
